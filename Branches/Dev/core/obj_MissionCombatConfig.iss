/* obj_MissionCombatConfig.iss - Contain all interaction with MissionCombat
config files (i.e. settingsref) so as to clean it up, make it easy to use,
and separate config crap from the combat missioner.*/

objectdef obj_MissionCombatConfig
{
	method Initialize()
	{
		Logger:Log["obj_MissionCombatConfig: Initialize not implemented."]
	}	
}
